apiVersion: v1
kind: Service
metadata:
  name: {{ include "docuquery.fullname" . }}
  labels:
    {{- include "docuquery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "docuquery.selectorLabels" . | nindent 4 }}
