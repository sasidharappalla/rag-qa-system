apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "docuquery.fullname" . }}
  labels:
    {{- include "docuquery.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
